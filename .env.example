# Control plane
DATABASE_URL=postgres://cloudable:cloudable@localhost:5442/cloudable
PORT=4780
BETTER_AUTH_SECRET=dev-only-change-me
BETTER_AUTH_URL=http://localhost:4780
# Console dev server origin, allowed via CORS — see apps/console/vite.config.ts
CONSOLE_ORIGIN=http://localhost:5180

# Which ProvisioningService this control-plane runs with: fake (default, no
# real infra) | docker (real local containers, dev/demo, no Azure account
# needed) | azure (stub — no real Azure account exists in this build).
PROVISIONING_ADAPTER=fake
# Where a Local Docker machine's agent reaches this control-plane from
# inside its container. host.docker.internal (not localhost) is the
# container's own host — works out of the box on Docker Desktop.
LOCAL_DOCKER_CONTROL_PLANE_URL=http://host.docker.internal:4780

# Cloud provider federation (see docs/cloud-auth.md) — must be the public URL
# a customer's Azure federated identity credential is configured to trust.
FEDERATION_ISSUER_URL=http://localhost:4780
FEDERATION_AUDIENCE=api://AzureADTokenExchange

# Agent (dev/local join-token flow — see docs/agents.md)
CONTROL_PLANE_URL=http://localhost:4780
MACHINE_TOKEN=

# CLI
CLOUDABLE_API_URL=http://localhost:4780
